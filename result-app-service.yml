apiVersion: v1
kind: Service
metadata:
    name: result-service
    labels:
        name: result-service
        app: sample-voting-app
spec:
    ports:
    -   port: 80
        targetPort: 80
    selector:
        name: result-app
        app: sample-voting-app
